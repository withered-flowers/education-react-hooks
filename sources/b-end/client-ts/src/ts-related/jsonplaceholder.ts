// Ini bukan tempat terbaik untuk menaruh enums

// Hanya untuk pembelajaran semua value yang berhubungan
// Dengan Typescript akan diletakkan di "folder ts-related"

export interface JsonPlaceholderResponse {
  userId: number;
  id: number;
  title: string;
  completed: boolean;
}
